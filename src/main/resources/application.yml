spring:
  application:
    name: licensing-service
  profiles:
    active: dev
  config:
    import: configserver:http://localhost:8071

management:
  endpoint:
    health:
      show-components: always
      show-details: always
  health:
    db:
      enabled: true
    diskspace:
      enabled: true
